"use client";
import { useEffect, useState } from "react";
export default function AdminSummaryClient() { const [data, setData] = useState<Record<string, unknown> | null>(null); const [error, setError] = useState(""); useEffect(() => { fetch("/api/admin/analytics/summary").then(async (response) => { const body = await response.json(); if (!response.ok) throw new Error(body.error ?? "讀取失敗"); setData(body); }).catch((reason: Error) => setError(reason.message)); }, []); return <section className="mt-8 rounded-2xl border border-[#C9D8DE] bg-white p-6"><h2 className="font-bold">分析摘要</h2>{error ? <p className="mt-4 text-sm text-[#B42318]">{error}</p> : data ? <pre className="mt-4 overflow-auto rounded-lg bg-[#F3F7F8] p-4 text-xs">{JSON.stringify(data, null, 2)}</pre> : <p className="mt-4 text-sm text-[#536168]">載入中…</p>}</section>; }
