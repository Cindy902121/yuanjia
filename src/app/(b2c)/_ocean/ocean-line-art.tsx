/**
 * 品牌故事 Section 左側留白用的低調線稿裝飾。
 *
 * 2026-09：原為 `/about-preview` 專用元件，Ocean Motion 視覺設計五輪確認
 * 核准後原封不動搬進 `_ocean/` 給正式首頁使用，內容／參數沒有變動。
 *
 * 使用者原本的意見：「品牌故事」左欄在大螢幕下只有一行小標＋一行標題，
 * 下面留白偏空——但不能為了填滿空間去動文字或版面位置，所以這裡另外疊一層
 * 「抽象海流」線稿在同一塊留白區，位置在文字後面（z-index 較低）、完全不
 * 影響文字排版本身。

 * 三條互相平行、緩緩起伏的單線（象徵洋流／等高線，不是具象的魚或網具），
 * single path、無填色、細線條，刻意故意做得比容器大一些並允許超出左邊界
 * （`-left-*`），讓它讀起來像背景紋理而不是一個「被放進來的裝飾物件」。
 *
 * 2026-09（使用者回報「線稿太淡幾乎看不見」後小幅調整）：opacity 從 0.07
 * 提高到 0.10——這個元素本來就設計成三者之中最內斂的一個（純抽象曲線，
 * 不是具象生物），所以還是維持在使用者給的 0.04–0.12 範圍偏低的一端，
 * 只是原本的 0.07 在實際螢幕上確實太接近「看不見」。
 *
 * 純 SVG、Server Component 即可渲染，不需要 "use client"。只在 `sm` 以上
 * 顯示——手機版本身沒有這塊留白問題（單欄版面已經填滿寬度），沒有必要
 * 在小螢幕上多渲染一層裝飾圖層。
 */
export function OceanLineArt() {
  return (
    <svg
      aria-hidden="true"
      viewBox="0 0 800 900"
      className="op-line-art pointer-events-none absolute -left-24 -top-10 hidden h-[720px] w-[820px] opacity-[0.1] sm:block"
    >
      <path
        d="M-50,150 C140,60 300,240 480,150 C630,80 730,190 850,120"
        fill="none"
        strokeWidth={1}
      />
      <path
        d="M-50,360 C160,270 320,450 500,360 C650,290 740,400 850,340"
        fill="none"
        strokeWidth={1}
      />
      <path
        d="M-50,580 C170,490 320,650 500,570 C640,500 730,610 850,560"
        fill="none"
        strokeWidth={1}
      />
    </svg>
  );
}
